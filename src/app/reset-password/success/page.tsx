import React from 'react';
import ResetSuccess from '../../components/ResetSuccess';

const ResetSuccessPage = () => {
  return <ResetSuccess />;
};

export default ResetSuccessPage;
// This page component renders the ResetSuccess component, which displays a success message after a password reset.
// It is used to confirm to the user that their password has been successfully reset.